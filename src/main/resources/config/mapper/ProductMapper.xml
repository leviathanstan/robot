<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.robot.dao.ProductDao">
    <resultMap id="productMap" type="com.robot.entity.Product" autoMapping="true">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="lastUpdateTime" column="post_date"/>
        <result property="coverImg" column="cover"/>
    </resultMap>
    <!-- 首页产品 -->
    <select id="getProductLibrary" resultMap="productMap">
        select id, name, pro_last_up_time, cover from product_library order by pro_last_up_time desc limit 0, 16;
    </select>
    <!--搜索-->
    <select id="findProductIf" parameterType="map" resultMap="productMap">
        select * from product_library where 1=1
        <if test="name!=null and name.trim().length()>0">
            and locate(#{content},name)>0 or local(#{content},introduction)>0
        </if>
    </select>
    <!--搜索统计-->
    <select id="getSearchCount" parameterType="java.lang.String" resultType="int">
        select count(id) from product_library where locate(#{content},name)>0 or local(#{content},introduction)>0
    </select>
</mapper>