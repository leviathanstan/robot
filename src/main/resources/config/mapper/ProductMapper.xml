<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.robot.dao.ProductDao">

    <resultMap id="productMap" type="com.robot.entity.Product">
        <id property="id" column="id"/>
        <result property="name" column="product_name"/>
    </resultMap>
    <!-- 首页产品 -->
    <select id="getProductLibrary" resultMap="productMap">
    select id, product_name from product order by pro_last_up_time desc limit 0, 16;
    </select>

    <resultMap id="articleMap" type="com.robot.entity.RobotNews" autoMapping="true">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="postDate" column="post_date"/>
    </resultMap>
    <!-- 首页产品评测 -->
    <select id="getProductEvaluation" resultMap="articleMap">
      select id, title, post_date from product_evaluate order by post_date desc limit 0, 9;
    </select>
    <!-- 首页产品新闻 -->
    <select id="getIndexNews" resultMap="articleMap">
      select id,title,post_date from product_news order by post_date limit 16
    </select>
    <!-- 首页产品推荐 -->
    <select id="getIndexRecommend" resultMap="productMap">
        select id,title, post_date from product_recommend order by post_date limit 16
    </select>
</mapper>